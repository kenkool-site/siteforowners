Add a new tab to the client dashboard. This must:
1. Create the page at src/app/(dashboard)/$ARGUMENTS/page.tsx
2. Add the nav item to the dashboard layout sidebar/bottom nav
3. Include proper auth guard and tenant scoping
4. Be mobile-first (card layout, not tables)
5. Use shadcn/ui components

Tab name: $ARGUMENTS
